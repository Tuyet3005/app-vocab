# app-vocab

Simple Node.js + Express app for entering English vocabulary lists.

Run:

```powershell
cd d:/_LapTrinh/app-vocab
npm install
npm start

# then open http://localhost:3000 in your browser
```

The UI accepts one vocab per line and saves to `data/vocabs.json`.
